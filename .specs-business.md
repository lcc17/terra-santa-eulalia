# Terra Santa Eulalia - Business Specs

## Completed Changes

### localization-sync (2026-04-15)

**Problem:** Locale detection desync — Home page showed ES content, Rituales showed EN content. Middleware was redirecting to `/ca` but context wasn't syncing properly, causing hydration mismatches.

**Solution Applied:**

| File | Change |
|------|--------|
| `middleware.js` | Added `x-next-locale` and `x-locale-detected` response headers for Netlify Edge Functions |
| `src/lib/context.js` | Added `mounted` state + `useEffect` sync from `NEXT_LOCALE` cookie and URL pathname to prevent hydration mismatch |
| `src/app/page.jsx` | Removed `console.log`, changed `pt-20` → `pt-24` |
| `src/app/rituales/page.jsx` | Added `pt-24` to main container |

**Verification:**
- Build succeeded (15 routes generated)
- No console.log statements remaining
- All images use Next.js `<Image />` component
- All links use Next.js `<Link />` component
- No raw `<a href>` found in codebase

**Locales:** Catalan (ca) as default, Spanish (es), English (en) supported.

**Status:** ✅ Ready for Netlify production deployment.
